(()=>{'use strict';const KEY='kreditplaner_v1';const euro=new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR'}),df=new Intl.DateTimeFormat('de-DE');function money(v){const n=Number(v);return Number.isFinite(n)?Math.max(0,Math.round(n*100)/100):0}function parse(s){if(!/^\d{4}-\d{2}-\d{2}$/.test(s||''))return null;const[y,m,d]=s.split('-').map(Number);return new Date(Date.UTC(y,m-1,d))}function fmt(s){const d=parse(s);return d?df.format(d):'—'}function addM(s,n){const d=parse(s);if(!d)return null;const day=d.getUTCDate(),t=new Date(Date.UTC(d.getUTCFullYear(),d.getUTCMonth()+n,1)),last=new Date(Date.UTC(t.getUTCFullYear(),t.getUTCMonth()+1,0)).getUTCDate();t.setUTCDate(Math.min(day,last));return `${t.getUTCFullYear()}-${String(t.getUTCMonth()+1).padStart(2,'0')}-${String(t.getUTCDate()).padStart(2,'0')}`}function projected(balance,rate,nextDue){if(balance<=0)return null;if(rate<=0||!nextDue)return null;return addM(nextDue,Math.max(0,Math.ceil(balance/rate)-1))}function monthsEarlier(oldDate,newDate){const a=parse(oldDate),b=parse(newDate);if(!a||!b)return 0;return Math.max(0,(a.getUTCFullYear()-b.getUTCFullYear())*12+(a.getUTCMonth()-b.getUTCMonth()))}function getLoan(id){try{const s=JSON.parse(localStorage.getItem(KEY)||'{"loans":[]}');return Array.isArray(s.loans)?s.loans.find(l=>l.id===id):null}catch{return null}}function style(){if(document.getElementById('forecast-style'))return;const s=document.createElement('style');s.id='forecast-style';s.textContent='.forecastBox{margin-top:12px;padding:14px;border:1px solid color-mix(in srgb,var(--a) 24%,var(--l));border-radius:17px;background:linear-gradient(135deg,color-mix(in srgb,var(--c) 88%,var(--a) 12%),color-mix(in srgb,var(--c) 94%,var(--a2) 6%))}.forecastTitle{font-size:14px;font-weight:800;color:var(--a);margin-bottom:3px}.forecastHint{font-size:11px;color:var(--m);line-height:1.35;margin-bottom:11px}.forecastInputRow{display:flex;align-items:center;gap:8px;font-size:12px;color:var(--m);flex-wrap:wrap}.forecastInput{width:130px;min-height:40px;border:1px solid var(--l);border-radius:11px;padding:7px 10px;background:var(--c);color:var(--t);font-size:15px;font-weight:700}.forecastResult{margin-top:11px;border:1px solid color-mix(in srgb,var(--a) 25%,var(--l));border-radius:14px;padding:11px;background:color-mix(in srgb,var(--c) 94%,var(--a) 6%)}.forecastLead{font-size:13px;font-weight:800;color:var(--t)}.forecastLead strong{color:var(--g)}.forecastGrid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:9px}.forecastMetric small{display:block;color:var(--m);font-size:10px;margin-bottom:3px}.forecastMetric b{font-size:14px}.forecastNote{font-size:10px;color:var(--m);margin-top:9px}.forecastEmpty{color:var(--m);font-size:12px}@media(max-width:430px){.forecastGrid{grid-template-columns:1fr}}';document.head.appendChild(s)}function renderBox(card){if(card.querySelector('.forecastBox'))return;const id=card.dataset.id,loan=getLoan(id);if(!loan)return;const det=card.querySelector('.loanDet'),actions=det?.querySelector('.actions');if(!det||!actions)return;const box=document.createElement('div');box.className='forecastBox';box.innerHTML='<div class="forecastTitle">↗ Prognose: Sonderzahlung</div><div class="forecastHint">Sieh, wie sich eine zusätzliche Zahlung auf dein voraussichtliches Enddatum auswirkt.</div><div class="forecastInputRow"><span>Wenn ich heute</span><input class="forecastInput" type="number" min="0" step="10" inputmode="decimal" placeholder="500"><span>€ extra zahle …</span></div><div class="forecastResult"><div class="forecastEmpty">Betrag eingeben, um die Prognose zu sehen.</div></div><div class="forecastNote">Die Prognose basiert auf unveränderter Monatsrate und berücksichtigt keine zusätzlichen Zinsen oder Gebühren.</div>';det.insertBefore(box,actions);const input=box.querySelector('.forecastInput'),result=box.querySelector('.forecastResult');function calc(){const l=getLoan(id)||loan,bal=money(l.balance),rate=money(l.rate),amt=Math.min(money(input.value),bal),oldEnd=projected(bal,rate,l.nextDue);if(!amt){result.innerHTML='<div class="forecastEmpty">Betrag eingeben, um die Prognose zu sehen.</div>';return}const newBal=money(bal-amt),newEnd=newBal<=0?null:projected(newBal,rate,l.nextDue),earlier=oldEnd&&newEnd?monthsEarlier(oldEnd,newEnd):oldEnd&&newBal<=0?Math.max(1,Math.ceil(bal/rate)-1):0;let lead=newBal<=0?'Mit dieser Zahlung wärst du sofort schuldenfrei.':earlier>0?`… bin ich ca. <strong>${earlier} Monat${earlier===1?'':'e'} früher</strong> fertig!`:'Das Enddatum bleibt voraussichtlich gleich.';result.innerHTML=`<div class="forecastLead">${lead}</div><div class="forecastGrid"><div class="forecastMetric"><small>Neues voraussichtliches Datum</small><b>${newBal<=0?'Heute':fmt(newEnd)}</b></div><div class="forecastMetric"><small>Neue Restschuld danach</small><b>${euro.format(newBal)}</b></div></div>`}input.addEventListener('input',calc)}function apply(){style();document.querySelectorAll('#loanList .loan').forEach(renderBox)}const root=document.querySelector('#loanList');if(root){new MutationObserver(()=>apply()).observe(root,{childList:true,subtree:true});apply()}else{document.addEventListener('DOMContentLoaded',()=>{const r=document.querySelector('#loanList');if(r){new MutationObserver(()=>apply()).observe(r,{childList:true,subtree:true});apply()}})}})();